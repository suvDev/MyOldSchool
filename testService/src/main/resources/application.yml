# Discovery Server Access
spring:
  application:
    name: test-service

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:5000/eureka/
    instance:
      hostname: localhost

# HTTP Server
server:
  port: 4445   # HTTP (Tomcat) port