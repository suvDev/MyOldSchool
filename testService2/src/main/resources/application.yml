# Discovery Server Access
spring:
  application:
    name: inter-comm

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
    instance:
      hostname: localhost

# HTTP Server
server:
  port: 4446   # HTTP (Tomcat) port